# wp-smtp-mailer

A simple WordPress plugin that lets you:

- Configure any SMTP server from a clean settings UI  
- Override all WordPress `wp_mail()` emails  
- Send custom emails using a built-in email editor  

All lightweight, no bloat, single-file plugin.
